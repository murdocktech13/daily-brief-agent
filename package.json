{
  "name": "daily-brief-agent",
  "version": "1.0.0",
  "description": "Autonomous agent that reads news, writes a brief with Claude, and saves it to Notion",
  "main": "agent.ts",
  "scripts": {
    "start": "tsx agent.ts",
    "dev": "tsx --watch agent.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "@notionhq/client": "^2.2.15",
    "dotenv": "^16.4.5",
    "rss-parser": "^3.13.0",
    "tsx": "^4.19.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
